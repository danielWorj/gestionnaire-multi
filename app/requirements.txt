# --- Core Flask ---
Flask==3.0.0
Werkzeug==3.0.1

# --- Base de données / ORM ---
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5

# --- Serialisation / validation ---
marshmallow==3.20.1
flask-marshmallow==1.2.1
marshmallow-sqlalchemy==0.29.0

# --- Authentification JWT (module Authentification) ---
Flask-JWT-Extended==4.6.0

# --- CORS (necessaire si le frontend appelle l'API depuis un autre domaine/port) ---
Flask-CORS==4.0.0

# --- Configuration ---
python-dotenv==1.0.0

# --- Driver base de donnees (MySQL / MariaDB) ---
PyMySQL==1.1.0

# --- Generation des bulletins PDF (module Evaluation, note N2) ---
WeasyPrint==62.3